// This makes it so that constants are consistently defined across files. 
class Params {
    // Variables for simulation state; 
    constructor() {
      this.deltaT = 0.03;
    }
}

var SceneParams = new Params();